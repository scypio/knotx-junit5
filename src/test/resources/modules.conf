test.random {
  sampleServer {
    port = 4000
    methods = GET|POST
  }
}

modules = [
  "server=io.knotx.junit5.utils.MockServerVerticle"
]

config.server.options.config {
  port = ${test.random.sampleServer.port}
}
