# this is random port
test.random.sampleServer.port = 0

modules = [
  "server=io.knotx.junit5.utils.TestConfigurationHttpServer"
]

config.server.options.config {
  port = ${test.random.sampleServer.port}
  extensionConfig = ${test}
}
