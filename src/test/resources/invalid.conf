# WireMock servers section
test.random {
  # all ports will be randomized and overwritten
  dynamicPortServer {
    port = 4000
    methods = GET|POST
  }
}

test.wiremock {
  # the same server name
  dynamicPortServer = 1234
  staticPortServer = 1234
}
