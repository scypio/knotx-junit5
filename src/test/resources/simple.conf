# WireMock servers section
test.random {
  # all ports will be randomized and overwritten
  dynamicRandomPortServer {
    port = 4000
    methods = GET|POST
  }
}

test.wiremock {
  dynamicWiremockPortServer = 0
  staticPortServer = 1234
}
